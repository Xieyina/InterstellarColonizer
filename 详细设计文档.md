# 详细设计文档

> 项目：星际殖民者（Interstellar Colonizer）　|　版本：v1.0　|　日期：2026-09-10
> 对应代码快照：JavaRTS-main（无 git 仓库，为 2026-09-10 文件快照；含四批合规修复与第五批测试代码）
> 本文档所有类名、方法名、数值均已与 `src/main/java` 实际代码核对；类图引用 `resources/classDiagram.puml`（第二批 T-08 已与代码同步）。

## 1. 架构总览

项目采用 MVC 分层架构，依赖方向严格单向：**View → Controller → Model**。

![架构组件图](images/arch_component.png)

- **View（JavaFX 界面层）**：`MainView`、`MapView`、`CustomMenu`、`BuildingFooter`、`PeopleFooter`、`TechView`、`ChartView`、`SaveManager`、各类弹窗；以及 `FxGameClock`、`FxGameUiBridge`（Model 端口的 JavaFX 实现）。
- **Controller（命令层）**：`BagOfCommands`（单例命令队列）、`Controller`、`Command` 接口与 13 个具体命令。
- **Model（业务逻辑层）**：`GameManager`（单例门面）、`BuildingManager`、`ResourceManager`、`TechTree`、`EventManager`、`People` 等；**不含任何 JavaFX 依赖**。
- **Model.Port（解耦端口）**：`GameClock`、`GameUiBridge` 两个接口。Model 只依赖自己定义的端口，View 提供实现并在 `MainApp.start` 中通过 `GameManager.bootstrap()` 注入——这是 T-06/T-07 改造的核心：Model 层零 `javafx` import、零 `View` import（已 grep 验证 0 命中）。

## 2. 类图

![完整类图](images/classDiagram.png)（SVG 版：`images/classDiagram.svg`）

按包分组说明：

| 包 | 内容 |
|---|---|
| `Model/Building` | `Building` 接口 + `BasicBuilding` 基础实现 + 4 个装饰器（`ProductionBuilding`/`ConsumptionBuilding`/`LivingBuilding`/`WorkingBuilding`）+ `BuildingManager` + `BuildingBuilder` + `BuildingType`（10 值）/`BuildingFunction` 枚举 |
| `Model/Building/State` | 状态机：`State` 接口、`AbstractState`、`Automata`、`States` 枚举与 5 个具体状态类（`ConstructionState`/`RunningState`/`BlockedState`/`BoostState`/`BrokenState`） |
| `Model/Resource` | 9 种资源类 + `ResourceType` 枚举（9 值）+ `ResourceManager` + `RecipeRegistry` + `ResourceConverter` |
| `Model/Tech` | `TechTree`（11 节点）、`TechNode`、`TechEffect` |
| `Model/Event` | `GameEvent` 接口、`EventOption`、`EventManager` 与 15 个事件类 |
| `Model` 根 | `GameManager`、`People`、`Map`（200×200）、`HistoricalData`、`Subject`、`Position`/`Size`；`Model/Port` 两个接口 |
| `View` | 界面类与 `Observer` 接口、`FxGameClock`/`FxGameUiBridge` |
| `Controller` | `BagOfCommands`、`Controller`、`Command` 接口 + `Commands/` 13 个命令类 |
| `Exception` | 10 个业务异常 |

## 3. 时序图

### 3.1 每日结算

![每日结算时序图](images/seq_daily_settlement.png)

流程说明：`FxGameClock` 的 `Timeline` 每 600 秒触发一次（FX 线程只做调度），结算本体提交到单线程守护执行器 `daily-settlement`；结算顺序严格为：事件弹窗短路 → `BuildingManager.handle()`（建筑产出/消耗 + 资源不足自动阻断）→ `RecipeRegistry.processAll()`（已解锁配方转化）→ `EventManager.checkAndTrigger()`（随机事件，弹窗回 FX 线程）→ 每 10 天 `recordHistory()` → `updateFatigue()` → `checkFoodAndApplyHunger()` → `notifyListener()`（经 `GameUiBridge.dispatch` 回 FX 线程刷新 UI）。结算中所有触碰 JavaFX 节点的操作均通过 `GameUiBridge` 回到 FX 线程。

### 3.2 建筑放置

![建筑放置时序图](images/seq_build.png)

流程说明：地图点击只入队一条 `AddBuildingCommand`；后台命令线程执行时依次经过科技解锁校验（`TechTree.isBuildingUnlocked`）、建造者构建（`BuildingBuilder` 用装饰器组合功能）、地块空闲校验、资源校验与扣减（`BuildingManager.addBuilding` → `ResourceManager.areAvailable`），最后 `Map.construct` 占用地块；新建筑构造即进入 `CONSTRUCTION` 施工状态。

### 3.3 分配工人

![分配工人时序图](images/seq_assign_worker.png)

流程说明：分配工人同样走命令队列；`GameManager.assignWorkerTo` 先找空闲居民，容量校验失败抛 `TooManyWorkers` 并顶部报错；成功后 `affectJobPlace` 建立人-岗关联。产出折算（Σ工人效率 / 最大工人数）在 `BuildingManager.handle()` 中生效，界面由 `notifyListener` 刷新。

### 3.4 存档与读档

![存档读档时序图](images/seq_save.png)

流程说明：`SaveManager.save` 从 `GameManager` 读取天数/资源/人口/建筑数/已研究科技/已解锁配方组装 `SaveData`，经 Jackson `writerWithDefaultPrettyPrinter` 写入 `saves/<槽位>.json`；读档反向恢复。**已知局限如实标注：`SaveData` 只保存 `buildingCount`，不保存建筑种类与地图坐标，读档后建筑布局无法还原。**

## 4. 状态图：建筑生命周期

![建筑状态图](images/state_building.png)

转换条件表（与 `BasicBuilding.handleState` / `switchState` 一致）：

| 转换 | 触发条件 |
|---|---|
| 放置 → 施工 CONSTRUCTION | `BasicBuilding` 构造时进入 |
| 施工 → 运行 RUNNING | 施工倒计时归零（每日 `handle()` 递减，自动） |
| 运行 → 阻断 BLOCKED | 玩家暂停（`blockBuilding`）或资源不足自动阻断（`applyGlobalResources` 捕获 `NotEnoughResources`） |
| 阻断 → 运行 | 玩家手动恢复（`runBuilding`） |
| 运行 → Boosted | 玩家 Boost（`boostBuilding`：消耗 1 工具，持续 5 个游戏日） |
| Boosted → 运行 | 倒计时归零且 `rand.nextInt(4) != 0`（3/4 概率） |
| Boosted → 损坏 BROKEN | 倒计时归零且 `rand.nextInt(4) == 0`（1/4 概率） |
| 运行 → 损坏 | 每日 `rand.nextInt(1000) == 0`（1/1000 概率） |
| Boosted → 阻断 | 玩家暂停（Boost 倒计时照常继续） |
| 损坏 → 运行 | 玩家修复（`repairBuilding`：消耗 1 工具） |

Boost 期间产出翻倍、消耗不变（`ProductionBuilding.handle` 中 `boost = 2`，`ConsumptionBuilding` 不随 Boost 变化）。

## 5. 设计模式说明（7 种，落地类均真实存在）

| 模式 | 落地类 | 解决的问题 |
|---|---|---|
| 单例 Singleton | `GameManager`、`BagOfCommands`、`Map`、`ResourceManager`、`RecipeRegistry`、`SaveManager` | 全局唯一的游戏状态、命令队列、地图与资源库存 |
| 观察者 Observer | `Subject`、`View/Observer`（`MainView` 实现） | Model 变化通知 View 刷新（`notifyListener`/`notifyErrorListener`，经 `GameUiBridge.dispatch` 回 FX 线程） |
| 命令 Command | `Command` 接口 + `Controller/Commands/` 13 个命令 + `BagOfCommands` | 把建造/分配/暂停/Boost/研究等操作封装为可排队对象，UI 只入队、不直接改 Model |
| 装饰器 Decorator | `BuildingDecorator` + `ProductionBuilding`/`ConsumptionBuilding`/`LivingBuilding`/`WorkingBuilding` | 动态组合建筑功能（如 FARM = 居住+生产+工人），避免为每种组合建子类 |
| 建造者 Builder | `BuildingBuilder` | 集中构造 10 种建筑，隐藏装饰器组合细节 |
| 状态 State | `State`、`AbstractState`、`Automata`、`States` + 5 个具体状态类 | 建筑生命周期状态流转，各状态只处理自己允许的转换 |
| 注册表 Registry | `RecipeRegistry`（`registerRecipe`/`unlockRecipe`/`processAll`） | 配方集中注册与查询，每日结算统一执行已解锁配方 |

## 6. 关键设计决策

1. **Model 不直接用 `Timeline`，而是定义 `GameClock` 端口**：原因——Model 层若 import JavaFX 会违反分层（评分硬性扣分项），且时钟实现可替换（测试中可用 Mock 注入，见 `GameManagerTest`）。
2. **建筑用装饰器而非继承**：10 种建筑 × 4 种功能（生产/消耗/居住/工人）的组合若用继承会产生大量子类；装饰器让 `BuildingBuilder` 按需叠加功能层。
3. **资源定为 9 种并重写 PRD 数据字典**：代码实际实现 FOOD/WOOD/STONE/COAL/IRON/STEEL/CEMENT/LUMBER/TOOLS，PRD 原写 4 种（含不存在的 ore/energy）；按"文档向代码靠"原则修正（T-11）。
4. **游戏日 = 600 现实秒**：所有生产/消耗按"游戏日"结算，拉长每日时长即可等比放慢现实速率，数值平衡无需逐条调整（T-10）。
5. **每日结算移出 FX 线程**：结算本体提交到单线程守护执行器，Timeline 留在 FX 线程只做调度，`settling` 标志防重入；所有 UI 操作经 `GameUiBridge` 回 FX 线程。

## 7. AI 使用与核对说明

见 `docs/AI使用与核对说明.md`（第二批 T-05 产物，覆盖需求分析/详细设计/代码实现/答辩四环节；工具与提示词等真实信息由组员回填，未编造）。

## 8. 已知问题与局限

1. `INTERSTELLAR_TRAVEL` 科技的 `TechEffect` 为占位实现（`unlockBuilding(WOODENCABIN)`），真正的胜利触发在 `GameManager.researchTech`。
2. 存档不保存建筑种类与地图坐标（只存 `buildingCount`），读档后建筑布局无法还原。
3. **`GameManager.removeBuilding` 存在缺陷（测试暴露，未修主代码）**：先移出建筑集合再清理居民/工人，导致清理循环死循环；对无 WORKING 功能的建筑调用 `getWorkers()` 会抛异常。详见 `docs/单元测试报告.md` 第 5 节，待人工决定修复。
4. `model_UML.png`/`view_UML.png`/`controller_UML.png`/`exception_UML.png` 无对应 `.puml` 源文件，未重新导出。
5. 猎人工会图标为占位图（复用农场图标）；200×200 地图首帧绘制较慢；项目路径含中文时可能黑屏。
