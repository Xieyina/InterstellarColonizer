# 基线改造对照表

> 本表用途：答辩时每位组员能讲清"我改了哪个文件、为什么改、改前改后行为差异"。
> 「本组改动」列仅收录可从代码注释（`重构：`/`新增：` 前缀）、`REFACTOR_CHANGES_2.0.md` 及基线 UML（`resources/classDiagram.puml`）核实的事实；无法确定的内容一律标注 `TODO`，严禁编造。

| 文件 | 原基线行为 | 本组改动 | 改动原因 | 责任人 | 对应需求条目 |
|---|---|---|---|---|---|
| `Model/GameManager.java` | 每日推进间隔 1000ms（1 现实秒 = 1 游戏日）；食物不足时由 `ResourceManager` 立即屠杀人口/结束游戏；无科技树、随机事件、历史记录、猎物交付入口 | 时间缩放：`TIME_SCALE=600`、`DAY_DURATION_MS=600000`（1 游戏日 = 10 现实分钟）；新增饥饿 Debuff（`HUNGER_EFFICIENCY=0.5`、`HUNGER_POPULATION_LOSS_PER_DAY=1`）替代瞬间团灭；新增科技研究入口与胜利判定（`INTERSTELLAR_TRAVEL`）；新增事件弹窗调度（`eventDialogShowing` 暂停每日推进）；新增每 10 天 `recordHistory()`；新增 `deliverPreyToGuild()`、`updateFatigue()`、`addPopulation()`/`setDay()`（读档支持） | 需求要求拉长游戏日；饥饿机制改为 Debuff + 逐步流失；支持科技树/随机事件/图表/存档等扩展玩法 | TODO | FR-01、FR-04、FR-05 |
| `Model/Building/BuildingManager.java` | 生产结算仅按「在岗工人数量比例」折算，无科技加成、无等级加成、无疲劳加权；无猎人工会结算 | `handle()` 应用科技产出倍率、建筑等级加成（每级 +20%）、工人疲劳效率加权与饥饿效率倍率；新增 `HUNTER_PREY_FOOD_YIELD=5` 猎物→食物结算；`isBuildable()` 增加科技解锁检查；`HUNTERGUILD` 设为默认可建造 | 与科技树、疲劳、饥饿、猎人工会等新系统联动，统一在生产结算入口生效 | TODO | FR-04、FR-06 |
| `Model/Building/BasicBuilding.java` | 建筑无等级概念，名称固定；无猎物库存 | 新增建筑等级 1~5（`MAX_LEVEL=5`）：`getUpgradeCost()` = STEEL×level×30 + CEMENT×level×15，`canUpgrade()`/`upgrade()`，等级 >1 时名称后缀 ` Lv.<level>`；新增 `preyStock`/`deliverPrey()`/`getPreyStock()`/`consumePreyStock()` | 需求要求建筑可升级；猎人工会需要「按交付猎物结算」的数据通道 | TODO | FR-06 |
| `Model/People.java` | 居民仅有 `id`/`house`/`job` 三个字段，无状态系统 | 新增疲劳常量（`FATIGUE_PER_DAY=25`、`FATIGUE_RECOVERY_PER_DAY=25`、`FATIGUE_MAX=100`、`FATIGUE_THRESHOLD=50`、`FATIGUED_MOVEMENT_SPEED=0.5`、`FATIGUED_EFFICIENCY=0.5`）与字段（`consecutiveWorkDays`、`fatigue`、`movementSpeed`）；新增 `advanceDay()`/`getEfficiency()` | 需求要求居民疲劳系统：连续工作累积疲劳、效率与移速下降、休息恢复 | TODO | FR-03、FR-04 |
| `Model/Resource/ResourceManager.java` | 食物不足时在 `addResource` 内直接清零并引发后续屠杀/结束逻辑 | 食物不足仅清零并记录缺口，饥饿 Debuff 交由 `GameManager.checkFoodAndApplyHunger()` 处理；新增 `getResourceAmount()`、`setResourceAmount()`（读档恢复）；`areAvailable()` 支持 `Map` 参数 | 饥饿机制从「即时死亡」改为「Debuff + 逐步流失」，职责移交 GameManager | TODO | FR-04、FR-05 |
| `Model/Resource/RecipeRegistry.java` | 基线无此文件，无配方转化机制（基线类图无此类） | 整个文件为新增：注册 4 个配方（`ALLOY_SMELTING`、`LUMBER_PROCESSING`、`ADVANCED_FOOD`、`CEMENT_MIXING`）；`processAll()` 在每日结算中执行已解锁配方转化；支持配方解锁（科技） | 科技树需要「解锁配方」效果；扩展资源转化玩法 | TODO | 无（新增功能，PRD 待补写，TODO） |
| `Model/Tech/TechTree.java` | 基线无此文件，无科技系统（基线类图无 `Tech` 包） | 整个文件为新增：11 个科技节点分 4 级，前置依赖 + 资源消耗研究；6 种效果类型（解锁建筑/配方、单资源产出加成、全局产出加成、降耗、扩容）；`forceResearch()` 供事件奖励与读档 | 需求要求科技树系统，研究「星际航行」触发胜利 | TODO | 无（新增功能，PRD 待补写，TODO） |
| `Model/Event/EventManager.java` | 基线无此文件，无随机事件（基线类图无 `Event` 包） | 整个文件为新增：注册 15 种事件；每 5~14 个游戏日（`5 + random.nextInt(10)`）从「可触发事件」中随机选一个触发；事件弹窗期间暂停每日推进 | 需求要求随机事件系统，增加经营变数 | TODO | 无（新增功能，PRD 待补写，TODO） |
| `View/MainView.java` | 底部仅「建筑」「人员」两个 Tab 按钮；无 CSS 主题；建筑卡片可用性不检查科技 | 新增「🔬 科技树」「📊 图表」按钮（打开 `TechView`/`ChartView`）；新增加载 `dark-theme.css`（失败回退默认样式）；`setAvailability()` 增加科技解锁检查（未解锁卡片置灰禁用） | 为科技树与历史折线图提供入口；统一深色主题；未解锁建筑不可建造 | TODO | FR-02、FR-07 |
| `View/MapView.java` | 以 `GridPane` 逐格创建 200×200 个 `ImageView` 绘制地图，每格绑定悬停与点击事件；建筑按状态渲染 sprite | 无改动（`REFACTOR_CHANGES_2.0.md` 注明「界面（View）基本保持原样」） | 无改动，仅保留原实现 | TODO | FR-02 |
| `View/CustomMenu.java` | 顶部资源栏仅展示资源数量与错误提示区 | 新增「📅 Day N」天数显示；新增「💾 保存」「📂 加载」按钮（调用 `SaveManager`） | 展示游戏日进度；为存档读档功能提供界面入口 | TODO | FR-01、FR-02 |
