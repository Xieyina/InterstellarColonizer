# 答辩 PPT 大纲（星际殖民者 · 8 分钟讲 + 3 分钟答）

> 使用说明：每页「要点」为 PPT 页面内容，「讲稿」为口语化台词（可直接照读）。
> 配图引用 `docs/images/` 下图片；组员分工页与真实数据（姓名/学号）由组员回填，勿编造。
> 可选：如需 `.pptx` 骨架，由组员确认后按 `docs/_backup/build_pptx.py` 思路生成（本批未生成 pptx）。

---

## 第 1 页 · 封面
- 要点：项目名《星际殖民者 Interstellar Colonizer》；组员姓名+学号（TODO：由组员填写）；日期 2026-09-10；课程名。
- 讲稿：大家好，我们组做的是星际殖民者——一个基于 JavaFX 的资源管理策略游戏。今天用 8 分钟讲清三件事：架构怎么分层、模式怎么落地、代码质量怎么保证。（~60 字）

## 第 2 页 · 一句话定位
- 要点：30 秒电梯陈述：玩家建造经营殖民地，平衡 9 种资源与人口；Java 21 + JavaFX + Maven；亮点：科技树、随机事件、存档、建筑状态机。
- 讲稿：一句话说清这个项目：你在 200×200 的地图上经营殖民地，造建筑、分工人、研究科技、应对随机事件，每 10 分钟推进一个游戏日。技术栈是 Java 21 加 JavaFX，用 Maven 构建，重点在分层架构和 7 种设计模式。（~90 字）

## 第 3 页 · 需求范围
- 要点：PRD 的 FR-01~FR-14（P0 全部）；本次交付包含 14 条功能需求；明确不包含：主菜单、结算界面、联机、难度递增（见 PRD 4.2 非目标）。
- 讲稿：需求来自 PRD 的十四条功能需求，从建房子、分工人到科技树、随机事件、存档，全部 P0。没做的也很明确：主菜单、结算界面、联机这些都在非目标清单里。（~70 字）

## 第 4 页 · 架构总览
- 要点：引用 `images/arch_component.png`；MVC 三层 + 依赖方向 View→Controller→Model；Model 通过 GameClock/GameUiBridge 端口解耦，零 JavaFX 依赖。
- 讲稿：架构是严格的单向依赖：View 到 Controller 到 Model。特别讲一下，Model 层不直接引用 JavaFX，而是定义了两个端口接口，时钟和弹窗都由 View 注入——这是本组改造的重点。（~75 字）

## 第 5 页 · 设计模式（1/2）
- 要点：装饰器 + 状态机。装饰器：4 个功能层动态组合（FARM = 居住+生产+工人），为什么不用继承？状态机：5 状态 + Automata，转换条件表。
- 讲稿：建筑为什么用装饰器不用继承？十种建筑乘四种功能，继承会写四十个类；装饰器按需叠加功能层。状态机管生命周期：施工、运行、阻断、强化、损坏，每个转换都有明确触发条件。（~80 字）

## 第 6 页 · 设计模式（2/2）
- 要点：命令 + 观察者 + 单例。命令：13 个命令类排队执行，UI 只入队不改 Model；观察者：Subject/Observer 通知刷新；单例：GameManager 等 6 个。
- 讲稿：点击地图不是直接建房子，而是往命令队列里投一条命令，后台线程依次执行——这样 UI 和业务完全解耦。Model 变化用观察者通知界面刷新，全局状态用单例管理。（~75 字）

## 第 7 页 · 核心机制：每日结算
- 要点：引用 `images/seq_daily_settlement.png`；一个 tick 的顺序：建筑结算→配方转化→随机事件→疲劳→饥饿→刷新；结算在独立线程，不卡界面。
- 讲稿：每个游戏日的结算有固定顺序：先算建筑产出消耗，再跑配方，然后随机事件，最后疲劳和饥饿判定。这段逻辑我们移出了界面线程，还加了防重入，界面不会卡。（~70 字）

## 第 8 页 · 建筑生命周期
- 要点：引用 `images/state_building.png`；五状态转换表：Boost 1/4 概率损坏、运行中每日 1/1000 概率损坏等。
- 讲稿：建筑的五个状态互相转换：施工完自动运行，资源断了自动阻断，Boost 五天翻倍但结束有四分之一的概率损坏，坏了花一个工具修。这些概率都写在代码常量里。（~70 字）

## 第 9 页 · 扩展功能（超出原 PRD）
- 要点：科技树 11 节点（星际航行=胜利）；随机事件 15 种；JSON 存档。**明确标注：这三块是代码已实现、原 PRD 未覆盖，已在第三批补写进 PRD（FR-08~FR-14）**。
- 讲稿：除了基础玩法，还有三块扩展：十一节点科技树、十五种随机事件、JSON 存档。这三块最早不在 PRD 里，是开发中加出来的——我们后来按代码补齐了文档，避免"多做"被扣分。（~75 字）

## 第 10 页 · 代码质量改进
- 要点：真实测试数据：`Tests run: 42, Failures: 0, Errors: 0`（2026-09-10 实测，见 `docs/单元测试报告.md`）；改造对比：38 处调试输出→SLF4J；8 处空 catch→日志；4 万节点地图→可视区域懒加载；线程/监听器补释放。
- 讲稿：代码质量这块都是实测数字：四十二个单元测试全部通过。改造前有三十八处调试打印、八处吞异常、四万个地图节点；现在全部换成日志、补了资源释放，地图只渲染看得见的格子。（~80 字）

## 第 11 页 · 基线改造对照表摘要
- 要点：引用 `docs/基线改造对照表.md`：改了什么、为什么改、改前改后差异（每个组员对应自己的文件）。
- 讲稿：这张对照表是答辩自证的核心：每个文件改前什么样、我们改了什么、为什么改，都写清楚了，组员可以对着它讲自己负责的部分。（~60 字）

## 第 12 页 · AI 使用与核对说明
- 要点：引用 `docs/AI使用与核对说明.md`：四环节记录 + 人工核对结论；强调"所有 AI 产出都逐条核过代码"。
- 讲稿：我们用了 AI 辅助，但每个环节都有记录：需求、设计、代码、答辩四张表，AI 写了什么、人核对了什么、改了哪里。原则是 AI 只提速度，事实以代码为准。（~70 字）

## 第 13 页 · 组员分工（每人 1 页，模板）
- 要点：我负责的类 / 我做的改动 / 遇到的坑 / 怎么解决。（TODO：六位组员各填一页；改动依据见 `docs/基线改造对照表.md` 责任人列）
- 讲稿（模板）：我负责 XX 模块，改动前它是 XX，我改成了 XX，原因写在对照表里。中间踩过的坑是 XX，最后这样解决。（~55 字）

## 第 14 页 · 已知问题与改进方向
- 要点：3~5 条诚实列出：存档不还原建筑布局；星际航行效果是占位实现；删除有人的建筑有缺陷（测试已暴露，待修）；首帧地图较慢；猎人图标是占位。
- 讲稿：我们不回避问题：存档不还原建筑布局、终极科技的效果是占位、删除有人建筑还有个测试暴露的缺陷待修。这些都在文档里白纸黑字，也是下一步改进方向。（~70 字）

## 第 15 页 · Q&A 预案
- 要点：见下（12 题 + 答案要点，可做成附录页）。

---

## Q&A 预案（12 题）

1. **为什么游戏日 = 10 现实分钟？** 需求要求放慢节奏，所有数值按"游戏日"结算，只拉长一天长度就等比放慢，不用改任何平衡数值。
2. **为什么资源是 9 种而不是 PRD 的 4 种？** 代码实际实现了 9 种，PRD 里的 ore/energy 根本不存在；按"文档向代码靠"的原则重写了数据字典。
3. **Model 层为什么不能引用 JavaFX？** 分层规则：Model 是纯业务层，引用 JavaFX 会让业务绑定界面实现、无法独立测试；我们用 GameClock/GameUiBridge 端口注入替代。
4. **装饰器模式比继承好在哪？** 建筑功能（生产/消耗/居住/工人）是正交的，10 种建筑按需组合；继承要枚举所有组合，类会爆炸。
5. **状态机的 5 个转换条件分别是什么？** 见 PPT 第 8 页转换表：施工归零自动运行；断资源自动阻断；Boost 五天翻倍后 1/4 损坏；运行每日 1/1000 损坏；修复花 1 工具。
6. **命令队列是同步还是异步？** 异步：UI 线程只入队，后台线程依次取出执行，每两条之间隔 1ms；退出时 shutdown 中断线程。
7. **存档为什么只存部分数据？** 现在只存天数/资源/人口/建筑数/科技/配方；建筑的种类和坐标没存——这是真实局限，读档后布局不还原，文档里如实写了。
8. **科技树 11 个节点的前置关系？** 三级解锁链：三个初级互不依赖；高级采矿/冶金/高级农业/供应链是二级；高级能源、人工智能三级；星际航行要冶金+高级能源+人工智能三个前置，研究完就胜利。
9. **单元测试覆盖了什么、没覆盖什么？** 覆盖资源、配方、状态机、产出折算、升级、疲劳、饥饿、胜利判定共 42 例；没覆盖 UI 层和随机事件（概率分支），因为需要 JavaFX 环境和注入随机数。
10. **哪部分是你写的？改前改后有什么区别？** 对着基线改造对照表回答：我改了哪个文件、改前行为、改后行为、原因。
11. **AI 生成的代码怎么核对的？** 举一个实例：AI 建议的科技节点清单，我们逐个打开 TechTree.java 数出 11 个节点才写进 PRD；类图里的每个类名也都 grep 过源码。
12. **如果再做一遍会改什么？** 先把 removeBuilding 的缺陷修掉；存档直接序列化整个模型；给事件系统注入随机种子便于测试。

---

## 时间分配表（8 分钟，按 150 字/分钟）

| 阶段 | 页码 | 时长 |
|---|---|---|
| 开场 + 定位 + 需求 | 1~3 | 1 分 10 秒 |
| 架构 + 设计模式 | 4~6 | 1 分 50 秒 |
| 核心机制（结算/状态机/扩展） | 7~9 | 1 分 50 秒 |
| 质量与自证（测试/对照表/AI） | 10~12 | 1 分 50 秒 |
| 分工 + 已知问题 | 13~14 | 1 分 20 秒 |
| 合计 | 1~14 | **8 分钟**（讲稿全文约 1150 字 ≤ 1200 字） |
